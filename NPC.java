import java.util.Scanner;

public class NPC extends Character {
    public NPC(Scanner in, float version, String type) {
        super(in, version, type);
    }
    public Move getRandomMove(Character c, Place p) {

    }
}
